# NodeJS REST CRUD with MongoDB

### Set up project
```
npm init -y
npm install express body-parser cors --save
npm install @babel/core @bable/node @babel/preset-env --save-dev
npm install nodemon --save-dev
```
